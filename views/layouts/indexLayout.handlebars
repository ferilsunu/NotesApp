{{>header}}

<div class="app-layout">
    {{#if user}}
        {{>nav}}
    {{/if}}
    <div class="app-main-content">
        {{{body}}}
        {{#if user}}
            {{>footer-panel}}
        {{/if}}
    </div>
</div>

{{>footer}}



