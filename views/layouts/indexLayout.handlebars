{{>header}}

<div class="app-layout">
    {{#if user}}
        {{>nav}}
        <div class="sidebar-backdrop" id="sidebarBackdrop"></div>
    {{/if}}
    <div class="app-main-content">
        {{{body}}}
        {{#if user}}
            {{>footer-panel}}
        {{/if}}
    </div>
</div>

{{>footer}}



