<div class="content-page">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <div class="d-flex align-items-center justify-content-between mb-4 flex-wrap">
                        <div>
                            <div class="d-flex align-items-center mb-1">
                                <a href="/notebooks" class="text-muted mr-2 hover-underline">Notebooks</a>
                                <span class="text-muted mr-2">/</span>
                                <h3 class="custom-heading mb-0">{{notebook.name}}</h3>
                            </div>
                            <p class="text-muted mb-0 font-size-14">Notes categorized under {{notebook.name}}</p>
                        </div>
                        <a href="/addNote" class="btn btn-primary d-inline-flex align-items-center mt-2 mt-sm-0">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" class="mr-2"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
                            Create Note
                        </a>
                    </div>

                    <div class="table-responsive">
                        <table class="table tbl-server-info" id="myTable">
                            <thead>
                                <tr>
                                    <th scope="col">Title</th>
                                    <th scope="col">Notebook</th>
                                    <th scope="col">Created Date</th>
                                    <th scope="col" class="text-right">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each fetched_notes}}
                                <tr>
                                    <td>
                                        <a href="/SingleNote/{{_id}}" class="hover-underline">
                                            {{title}}
                                        </a>
                                    </td>
                                    <td>
                                        <span class="notebook-pill">{{notebook}}</span>
                                    </td>
                                    <td>
                                        <span class="text-muted">{{GenerateDate date 'DD MMMM YYYY'}}</span>
                                    </td>
                                    <td class="text-right">
                                        <div class="action-btn-group justify-content-end">
                                            <a href="/SingleNote/{{_id}}" class="action-btn edit-btn" title="Edit Note">
                                                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg>
                                            </a>
                                            <form action="/notes/{{_id}}?_method=DELETE" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this note?');">
                                                <button type="submit" class="action-btn delete-btn" title="Delete Note">
                                                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path></svg>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>